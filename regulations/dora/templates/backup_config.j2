{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 12(1) — Backup policies and restoration procedures #}
backup:
  frequency: "{{ backup_frequency | default('daily') }}"
  retention_days: {{ backup_retention_days | default(90) }}
  type: "{{ backup_type | default('incremental') }}"
  encryption: true
  verification:
    enabled: true
    schedule: "{{ verification_schedule | default('weekly') }}"
  restoration_tested: "{{ restoration_tested | default('') }}"
