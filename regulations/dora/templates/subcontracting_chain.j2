{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 29(2) — Subcontracting chain documentation #}
third_party:
  subcontracting_chain:
    primary_provider: "{{ primary_provider | default('') }}"
    subcontractors:
      - name: "{{ subcontractor_name | default('') }}"
        service: "{{ subcontractor_service | default('') }}"
        jurisdiction: "{{ subcontractor_jurisdiction | default('') }}"
        data_processing: "{{ data_processing | default('none') }}"
    last_reviewed: "{{ review_date | default('') }}"
    approval_required: true
