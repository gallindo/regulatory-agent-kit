{# Test template for DORA-IR-005 — Incident logging #}
{% if language == "java" %}
@Test
void {{ class_name | snake_case }}_shouldHaveIncidentLogging() {
    IncidentLogging annotation = {{ class_name }}.class.getAnnotation(IncidentLogging.class);
    assertNotNull(annotation, "{{ class_name }} must have @IncidentLogging per DORA Article 17(1)");
}
{% elif language == "python" %}
def test_{{ class_name | snake_case }}_has_incident_logging():
    assert hasattr({{ class_name }}, "_incident_logging_config")
{% endif %}
