{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 11(6) — RTO and RPO objectives #}
resilience:
  rto: "{{ rto | default('4h') }}"
  rpo: "{{ rpo | default('1h') }}"
  tier: "{{ resilience_tier | default('standard') }}"
  last_reviewed: "{{ effective_date }}"
  recovery_procedure_url: "{{ recovery_procedure_url | default('') }}"
