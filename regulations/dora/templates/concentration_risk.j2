{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 29(1)(b) — Concentration risk monitoring #}
third_party:
  concentration_risk:
    provider_count: {{ provider_count | default(0) }}
    critical_providers:
      - name: "{{ critical_provider | default('') }}"
        services_provided: []
        percentage_of_critical_functions: {{ percentage | default(0) }}
    risk_level: "{{ risk_level | default('not_assessed') }}"
    mitigation_plan: "{{ mitigation_plan | default('') }}"
    last_assessed: "{{ assessment_date | default('') }}"
