{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 8 — ICT asset identification #}
metadata:
  labels:
    ict-asset-id: "{{ asset_id | default('ASSET-PENDING') }}"
    ict-asset-classification: "{{ asset_classification | default('standard') }}"
    ict-asset-owner: "{{ asset_owner | default('') }}"
    dora-registered: "true"
