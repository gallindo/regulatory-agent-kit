{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 26 — TLPT infrastructure readiness #}
testing:
  tlpt:
    scope: "{{ tlpt_scope | default('critical_ict_systems') }}"
    last_conducted: "{{ tlpt_last_conducted | default('') }}"
    frequency: "{{ tlpt_frequency | default('every_3_years') }}"
    provider: "{{ tlpt_provider | default('') }}"
    methodology: "{{ tlpt_methodology | default('TIBER-EU') }}"
    results_classification: "{{ results_classification | default('confidential') }}"
