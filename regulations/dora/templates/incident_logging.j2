{# Generated by regulatory-agent-kit — {{ rule_id }} #}
{# DORA Article 17(1) — Incident logging annotation #}
{% if language == "java" %}
@IncidentLogging(
    category = "{{ incident_category | default('ict_service') }}",
    retentionDays = {{ retention_days | default(365) }}
)
{% elif language == "python" %}
@incident_logging(
    category="{{ incident_category | default('ict_service') }}",
    retention_days={{ retention_days | default(365) }},
)
{% endif %}
